{
  "name": "adam-naeman-portfolio",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "npx vite build",
    "vercel-build": "npx --yes vite build",
    "postinstall": "node -e \"try{require('fs').chmodSync('node_modules/.bin/vite',0o755)}catch(e){}\"",
    "preview": "vite preview",
    "sync:linkedin": "node scripts/sync-linkedin.mjs"
  },
  "devDependencies": {
    "vite": "^6.0.0"
  }
}
